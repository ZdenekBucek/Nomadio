import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";import type{TripPlaceRow}from"@/lib/supabase/database.types";vi.mock("./place-actions",()=>({createMapboxTripPlace:vi.fn(),createTripPlace:vi.fn(),removeTripPlace:vi.fn(),updateTripPlace:vi.fn()}));import{TripPlaces}from"./trip-places";
const place:TripPlaceRow={address:"Most 1",category:"nature",category_overridden:true,city:"Bodø",country_code:"NO",created_at:"2026-08-04T00:00:00Z",created_by:"user",id:"bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb",latitude:67.23,longitude:14.61,name:"Saltstraumen",provider:"manual",provider_category:null,provider_place_id:null,trip_id:"aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa",updated_at:"2026-08-04T00:00:00Z"};afterEach(cleanup);describe("TripPlaces",()=>{it("shows place and editor controls",()=>{render(<TripPlaces canEdit mapboxConfigured={false} places={[place]} tripId={place.trip_id}/>);expect(screen.getByText("Saltstraumen")).toBeInTheDocument();expect(screen.getAllByText("Příroda")).toHaveLength(3);expect(screen.getAllByText("Nabíjecí místo")).toHaveLength(2);expect(screen.getByText("Přidat vlastní místo ručně")).toBeInTheDocument();expect(screen.getByText("Upravit místo")).toBeInTheDocument();expect(screen.getByText("Vyhledávání míst zatím není připojené")).toBeInTheDocument()});it("renders a charging place with its own category",()=>{render(<TripPlaces canEdit={false} mapboxConfigured={false} places={[{...place,category:"charging",name:"Ionity"}]} tripId={place.trip_id}/>);expect(screen.getByText("Ionity")).toBeInTheDocument();expect(screen.getByText("Nabíjecí místo")).toBeInTheDocument()});it("keeps places read-only for viewer",()=>{render(<TripPlaces canEdit={false} mapboxConfigured={false} places={[place]} tripId={place.trip_id}/>);expect(screen.getByText("Saltstraumen")).toBeInTheDocument();expect(screen.queryByText("Upravit místo")).not.toBeInTheDocument();expect(screen.queryByRole("button")).not.toBeInTheDocument()})});
