import { cleanup, render, screen } from "@testing-library/react"; import { afterEach, describe, expect, it, vi } from "vitest"; import type { ItineraryItemRow } from "@/lib/supabase/database.types";
vi.mock("./item-actions",()=>({createItineraryItem:vi.fn(),moveItineraryItem:vi.fn(),removeItineraryItem:vi.fn(),updateItineraryItem:vi.fn()})); import { DayTimeline } from "./day-timeline";
const base={created_at:"2026-08-04T00:00:00Z",created_by:"user",day_id:"bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb",end_time:null,notes:null,place_id:null,updated_at:"2026-08-04T00:00:00Z"};
const items:ItineraryItemRow[]=[{...base,id:"1",item_type:"activity",title:"Chrám",start_time:"09:00:00",sort_order:0},{...base,id:"2",item_type:"note",title:"Koupit SIM",start_time:null,sort_order:1}]; afterEach(cleanup);
describe("DayTimeline",()=>{it("renders ordered items and editor controls",()=>{render(<DayTimeline canEdit dayId={base.day_id} items={items} places={[]} tripId="aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa"/>);expect(screen.getByText("Chrám")).toBeInTheDocument();expect(screen.getByText("09:00")).toBeInTheDocument();expect(screen.getByText("Přidat bod do timeline")).toBeInTheDocument();expect(screen.getAllByText("Upravit bod")).toHaveLength(2);});it("is read-only for viewer",()=>{render(<DayTimeline canEdit={false} dayId={base.day_id} items={items} places={[]} tripId="aaaaaaaa-aaaa-4aaa-8aaa-aaaaaaaaaaaa"/>);expect(screen.getByText("Koupit SIM")).toBeInTheDocument();expect(screen.queryByRole("button")).not.toBeInTheDocument();expect(screen.queryByText("Přidat bod do timeline")).not.toBeInTheDocument();});});
