# Feature modules

Each directory owns one business capability. Keep provider adapters outside the
domain model and expose the smallest practical public API. Modules are empty by
design until their implementation slice is approved.
