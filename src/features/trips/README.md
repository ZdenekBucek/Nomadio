# Trips

Trip metadata, memberships, travellers, sharing roles, and the unified trip list.

Trip settings include an opt-in `quick_expense_before_start_enabled` preference. Owner/editor members may expose a future, non-archived trip in the global Quick Expense action; the user-level FAB preference remains the master switch.
